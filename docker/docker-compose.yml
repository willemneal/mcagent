services:
  mcagent:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    networks:
      - mcagent-net

  install-test:
    build:
      context: ..
      dockerfile: docker/Dockerfile.install-test
    env_file:
      - .env
    volumes:
      - ..:/workspace:ro
      - ../docker:/docker:ro
    networks:
      - mcagent-net

networks:
  mcagent-net:
